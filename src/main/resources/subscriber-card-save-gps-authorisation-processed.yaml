zookeeper:
    host: ${zookeeper.host}
bootstrap:
    host: ${kafka.bootstrap.host}
topic:
   name:
      - com.orwellg.gps.authorisation.response.1
   commitInterval: 3
application:
   id: card-save-message-processed
   name: card-save-message-processed
configuration:
   autoOffsetReset: earliest