zookeeper:
    host: ${zookeeper.host}
bootstrap:
    host: ${kafka.bootstrap.host}
topic:
   name:
      - com.orwellg.gps.presentment.response.1
   commitInterval: 3
application:
   id: card-presentment-accounting-subscriber
   name: card-presentment-accounting-subscriber
configuration:
   autoOffsetReset: earliest