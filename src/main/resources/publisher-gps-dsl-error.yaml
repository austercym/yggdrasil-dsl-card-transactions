zookeeper:
    host: ${zookeeper.host}
bootstrap:
    host: ${kafka.bootstrap.host}
topic:
    name: 
       - com.orwellg.gps.presentment.error.1
    commitInterval: 3                         
application:
    id: gps-dsl-result-error-publisher
    name: gps-dsl-result-error-publisher
configuration:
    retries: 0
    acks: 1