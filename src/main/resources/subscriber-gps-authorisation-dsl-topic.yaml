zookeeper:
    host: ${zookeeper.host}
bootstrap:
    host: ${kafka.bootstrap.host}
topic:
   name: 
      - com.orwellg.gps.authorisation.request.1
   commitInterval: 3         
application:
   id: gps-dsl-topic-subscriber
   name: gps-dsl-topic-subscriber
configuration:
   autoOffsetReset: earliest