zookeeper:
    host: ${zookeeper.host}
bootstrap:
    host: ${kafka.bootstrap.host}
topic:
    name: 
       - com.orwellg.gps.authorisation.response.1
    commitInterval: 3                         
application:
    id: gps-dsl-result-success-publisher
    name: gps-dsl-result-success-publisher
configuration:
    retries: 0
    acks: 1